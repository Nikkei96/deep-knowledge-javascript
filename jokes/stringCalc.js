/**
 * * Здесь будет полноценный рофл-калькулятор 
 * @param {Number} a 
 * @param {Number} b 
 * @returns {Number}
 */
export default function (a, b) {
  return ' '.repeat(a).concat(' '.repeat(b)).length
}